export const selectCounter = (store: any) => store.counter.count;

export const selectInputValue = (store: any) => store.counter.inputValue;

export const selectErrorMessage = (store: any) => store.counter.errorMessage;